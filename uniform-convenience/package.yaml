name:         uniform-convenience
version:      0.0.9.6
license:      GPL
license-file: GPL_3
maintainer:   Andrew U. Frank <andrewufrank@gmail.com>
author:       Andrew Frank
copyright:    2021 Andrew U. Frank
category: Uniform
synopsis: convenience functions
description: some convenience functions, which are not good algebras
    and debatable
    exports in multiple modules

extra-source-files:
- README.md
- ChangeLog.md

dependencies:
- base >= 4.7 && < 5
- monads-tf
- safe
- text
- csv
- iso639
# - bytestring
# - pretty-show
# - MissingH
# - split
# - snap-core
# - text-icu
# - network-uri
# - test-invariant
# - numeric-prelude
# - uniform-algebras
#             bytestring -any,
#             directory ,
# - directory
# #             filepath -any,
# #             pureMD5 -any,
# - pureMD5
# - deepseq
# # - text
# #             deepseq,
# #             text -any,
# #             unix -any,
# - unix
# - pipes
# #             pipes ,
# #             safe,
# #             path ,
# - path
# - path-io
# - filepath
# #             path-io ,
# #             exceptions
# - exceptions
# #             , split
# - zlib
- uniform-strings
- uniform-error
- uniform-algebras
- uniform-fileio # for csv, which belongs someplace else
- uniform-time
- uniform-json  # another circle
# - uniform-time 
- HTF
# - test-invariant

library:
  source-dirs: src

# this is only a library, no directly executable
# executables:
#   primo-exe:
#     main:                Testing.hs
#     source-dirs:         app
#     ghc-options:
#     - -threaded
#     - -rtsopts
#     - -with-rtsopts=-N
#     dependencies:
#     - uniform-algebras

tests:
  strings-test:
    main:                Testing.hs
    source-dirs:         test
    ghc-options:
    - -threaded
    - -rtsopts
    - -with-rtsopts=-N
    dependencies:
    - uniform-algebras
    - HTF
    - test-invariant
    - quickcheck-text
    - uniform-strings
    - uniform-fileio
    - uniform-convenience
